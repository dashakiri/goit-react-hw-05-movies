import Navigation from '../navigation/Navigation';
import { Header } from '../appBar/App.styled';

export default function AppBar() {
  return (
    <Header>
      <Navigation />
    </Header>
  );
}
